apiVersion: v1
kind: Secret
metadata:
  name: backstage-secrets
  namespace: backstage
type: Opaque
stringData:
  # GitHub Personal Access Token (necessário para catalog)
  # Criar em: https://github.com/settings/tokens
  # Scopes: repo, read:org, read:user
  # IMPORTANTE: Substituir pelo token real antes de aplicar
  GITHUB_TOKEN: "REPLACE_WITH_GITHUB_TOKEN"

  # SonarQube Token
  # IMPORTANTE: Substituir pelo token real antes de aplicar
  SONARQUBE_TOKEN: "REPLACE_WITH_SONARQUBE_TOKEN"
